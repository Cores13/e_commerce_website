import React, {useEffect, useState} from 'react'
import {GlobalState} from '../../GlobalState'

interface Props {
    
}

export const MainPages: React.FC = ({}: Props) => {
    return (
        <div>
            Main Pages Component
        </div>
    )
}
